# Bug fixes — session 2026-08-08

Note để user copy đi check lại.

## 1. Text "Bước tiếp theo" cho nguồn MKT/MKT_BR/BDM
**File**: `resources/views/components/leads/⚡lead-form.blade.php:2446`
**Trước**: `Lead sẽ về kho team → chờ CM chia cho nhân viên booking.`
**Sau**: `Nhập phân loại, nguồn → Tele sale chuẩn bị.`

## 2. Panel "Trường bổ sung" hiển thị sai tab
**File**: `resources/views/components/leads/⚡lead-form.blade.php:2524`
**Bug**: Panel gắn `x-show="phase === 2"` (tab Gọi điện) → user nhập lead phải mở 2 tab mới điền hết.
**Fix**: Đổi thành `x-show="phase === 1"` (tab Tài khoản nhập lead) — nhập một lượt.

## 3. Banner cảnh báo Trực Page dùng text lỗi thời
**File**: `resources/views/components/leads/⚡lead-form.blade.php:2595`
**Trước**: `chỉ được điền "Trường bổ sung" ở tab Gọi điện`
**Sau**: `chỉ nhập được ở tab Tài khoản nhập lead (gồm cả "Trường bổ sung")`
**Lý do**: Consequence của fix #2 — text cũ trỏ sang tab sai.

## 4. SĐT bị strip nhầm mã "84"
**File**:
- `resources/views/components/leads/⚡lead-form.blade.php:2407-2408` (JS handler `x-on:paste` + `x-on:blur`)
- `app/Models/Lead.php:740` (`normalizePhone`)

**Bug**: Logic strip `84` không check độ dài. Input `842798596` (9 chữ số, đầu số Viettel `084...`) → strip `84` → còn `2798596` → prepend `0` → `02798596` (sai, mất số).
**Fix**: Chỉ strip `84` khi tổng độ dài = 11 (mã quốc gia `+84` + 9 chữ mobile chuẩn).

Verify:
- `842798596` → `0842798596` ✅
- `84987654321` (11 số, format +84) → `0987654321` ✅
- `0842798596` → `0842798596` ✅ (giữ nguyên)

## 5. Lead trong Kho số (pool_unit_id) vô hình với admin/CM/BO (Phase 6.24 regression)
**File**:
- `app/Models/Concerns/HasAccessControl.php` — thêm method `visiblePoolUnitIds()` + cache invalidation
- `app/Models/Lead.php:619+` (`scopeVisibleTo`) — thêm nhánh `orWhereIn('pool_unit_id', $poolUnitIds)`
- `app/Models/Lead.php:677+` (`isVisibleTo`) — thêm branch check `pool_unit_id`

**Bug gốc**: Phase 6.24 tách `pool_unit_id` (cây Kho số) khỏi `org_unit_id` (cây tổ chức) — lead do Trực Page up mode "Chia về kho" có `pool_unit_id=X, org_unit_id=NULL, pool_level=team`. Nhưng `scopeVisibleTo`/`isVisibleTo` **chỉ check org_unit_id** → mọi user trừ `imported_by` (chính Trực Page) không thấy. Admin/CM/BO đều mất hút lead MKT chia về kho.

**Fix**: `visiblePoolUnitIds()` resolve qua `org_pool_map` từ (visibleOrgUnitIds ∪ memberOrgUnitIds) → lead trong kho X visible với user có scope đến org đã map vào kho X.

Verify:
- Admin visible pools: 8 ✅
- hn.cms01 visible pools: [2,3,4] (kho HN) ✅
- Lead `KH-002-MKT` (pool_unit_id=3, org_unit_id=NULL) — admin thấy sau fix ✅

## 6. Banner "Phase X đã chốt" dài, technical
**File**: `resources/views/components/leads/⚡lead-form.blade.php:2368, :2585`
**Trước**: `Phase X đã chốt — chỉ đọc. Chỉ Admin vận hành (perm phase.rollback) mới sửa được.`
**Sau**: `Phase X đã chốt, liên hệ quản lý nếu cần chỉnh sửa.`

## 7. BO (Lễ Tân) thừa quyền lead.* — thấy Dashboard/Khách hàng/Thiết lập
**File**:
- `database/seeders/RolePermissionSyncSeeder.php:140-143` — bỏ 3 perm `lead.view`, `lead.view_phone`, `lead.distribute_sale` khỏi role "BO (Lễ Tân)".
- `app/Http/Controllers/AuthController.php:49+` — thêm `landingRouteFor()` — user không có report/lead perm nhưng có ups.view → login redirect thẳng `/ups-list` (không phải `/dashboard`).

**Bug**: BO Hà Nội (`bo.hn@longevity.com.vn`) là Lễ Tân, chỉ dùng UPS list, nhưng role gán thừa 3 perm lead.* → menu Dashboard/Khách hàng/Chia số/Thiết lập vẫn hiện + có thể click vào.

**Fix**: BO chỉ còn `ups.view/checkin/override/confirm_daily`. Menu tự ẩn do gate perm. Login redirect `/ups-list`.

**Verify**: bo.hn login → landing `/ups-list`, header chỉ có logo + avatar (không có menu lead-side).

## 8. Menu "Thiết lập" trùng chỗ giữa top nav và avatar dropdown
**File**: `resources/views/layouts/app.blade.php:52-53`
**Bug**: Top nav có dropdown "Thiết lập" (Trang thiết lập, Tổ chức & User, ...) trong khi avatar dropdown đã sẵn có mục "Cài đặt" (link vào cùng /settings tab-hóa) + "Quản lý phiên đăng nhập" — trùng chức năng.
**Fix**: Bỏ mục "Thiết lập" khỏi `$navItems`. Access qua avatar → Cài đặt (chỉ hiện với user có admin perm) là đủ. Mobile drawer cũng tự sạch vì iterate `$navItems`.

## 9. Menu — "Kho lead" đúng ra là chức năng của Khách hàng, "Chia số" chỉ còn UPS
**File**: `resources/views/layouts/app.blade.php:11-23, :49`

**Trước**:
- **Khách hàng** dropdown: Danh sách khách hàng | Thêm khách hàng | Duyệt Lead
- **Chia số** dropdown: UPS hôm nay | **Kho lead** | UPS check-in (BO)

**Sau**:
- **Khách hàng** dropdown: Danh sách khách hàng | Thêm khách hàng | **Kho lead** | Duyệt Lead
- **UPS list** dropdown (đổi tên từ "Chia số"): UPS hôm nay | UPS check-in (BO)

Lý do: kho lead là kho của khách hàng, thuộc menu Khách hàng. Menu "Chia số" chỉ còn 2 mục UPS → đổi tên thành "UPS list" cho đúng nội dung.

## 10. Lead MKT có tele rồi vẫn hiển thị "Chờ CM booking chia"
**File**: `app/Models/Lead.php:311+` (`initialPipelineFor`), `:274+` (`pipelineLabel`)

**Bug**: `initialPipelineFor()` cho nguồn MKT/MKT_BR/BDM luôn trả `PSTATUS_WAITING` bất kể có owner hay không. Trong `createLead()`, gọi này chạy SAU `poolAttributes()` → OVERWRITE giá trị `IN_CARE` mà pool đã set. Lead có tele auto-assign qua MKT List UPS vẫn stuck WAITING → label sai `"Booking · Chờ CM booking chia"`.

**Fix**:
- `initialPipelineFor(MKT/MKT_BR/BDM)` giờ return `IN_CARE` nếu có ownerId, `WAITING` nếu không.
- `pipelineLabel()`: khi WAITING + BOOKING phase + nguồn MKT/MKT_BR/BDM → hiện `"Chờ tele cập nhật"` (mode pool chờ tele nhặt), không còn "Chờ CM booking chia" (không còn CM chia trong flow mới).

**Backfill**: đã update tất cả lead MKT/MKT_BR/BDM có `owner_id` + `pipeline_status=waiting_distribute` → `in_care` (1 lead trong DB dev).

## 11. Dashboard sale thấy cả team, thiếu filter role
**File**: `resources/views/components/reports/⚡dashboard-overview.blade.php`

**Bug**: Sale (VD Nguyễn Trà My `hn.sale12`) mở Dashboard thấy 3 widget + bảng "Lead hôm nay" hiển thị số của **cả team** (visibleTo scope). Sale không có `lead.distribute` không cần thấy lead người khác. Ngoài ra 1 sale có thể vừa làm tele hôm nay vừa làm book ngày mai → cần filter role để lọc.

**Fix**:
- Thêm `isPersonalScopeOnly()` — user không có `report.view_all` + không có `lead.distribute` → chỉ thấy lead `owner_id = user OR receiver_id = user`.
- `reportLeadQuery()` áp scope mới cho sale-role; CM/Manager giữ `visibleTo` (thấy team).
- Thêm chip filter **Tất cả / Tele / Booking** (mapping: Tele = `phase=CF_PHASE_CALL`, Booking = `phase=CF_PHASE_BOOKING`) — áp cho cả bảng "Lead hôm nay" và badge count phía trên.

**Verify**: hn.sale12 trước fix `visibleTo=3`, sau fix personal-scope=1.

## 12. Chip filter Tele/Booking dùng nhầm cột (0 kết quả dù có lead)
**File**: `resources/views/components/reports/⚡dashboard-overview.blade.php` (`applyRoleFilter`)

**Bug**: Filter mapping trước dùng cột `phase` (Customer Flow 1-6): Tele=2, Booking=3. Nhưng Trực Page không có `lead.distribute` → tạo lead xong `phase` giữ nguyên = 1 dù đã auto-assign owner làm tele. Vào chip "Tele" → 0 kết quả mặc dù sale đang care.

**Fix**: Đổi mapping sang `pipeline_phase` (vai trò user đang giữ):
- Tele = `pipeline_phase='booking'` (chăm sóc để đặt lịch)
- Booking = `pipeline_phase='sale'` (đã book, chuyển sale close/tiếp đón)

**Verify**: hn.sale12 chip Tele giờ = 1 (đúng lead KH-012-MKT đang tele care).

## 13. Cột bảng "Lead hôm nay" khó hiểu + thừa "Trạng thái"
**File**: `resources/views/components/reports/⚡dashboard-overview.blade.php` (table header + row + eager load)

**Trước**: | Mã KH | Tên | SĐT | Nguồn | Phase | **Chia cho** | Trạng thái booking | **Trạng thái** (Mới/Follow/...) |
**Sau**:  | Mã KH | Tên | SĐT | Nguồn | Phase | **Tele phụ trách** | Trạng thái booking | **Sale tiếp đón** |

- "Chia cho" → "Tele phụ trách" (owner khi pipeline_phase='booking', fallback receiver).
- Thêm "Sale tiếp đón" (owner khi pipeline_phase='sale').
- Bỏ cột "Trạng thái" (classification Mới/Follow) — thừa, không ai đọc trên dashboard.
- Eager load thêm `receiver` (tránh N+1).

## 14. Phase 2 "Lưu thông tin" không tự nhảy sang phase 3
**File**: `resources/views/components/leads/⚡lead-form.blade.php` (`updateLead` ~line 1610)

**Bug**: `saveAndGoToBooking()` set session flag `go_to_booking_after_save`. `createLead()` honor flag → redirect `?phase=3`. Nhưng `updateLead()` bỏ qua flag → redirect về `leads.edit` không kèm phase → ở lại phase 2.

**Fix**: `updateLead()` thêm logic tương tự `createLead()` — nếu session flag on + user có `lead.book_action` → gán `$params['phase'] = 3` trước redirect.

## 15. "Lưu thông tin" phase 2/3 không tự chốt phase → không V xanh, title không update
**File**: `resources/views/components/leads/⚡lead-form.blade.php` (`saveAndGoToBooking`)

**Bug**: Sau khi user điền phase 2 (Gọi điện) bấm "Lưu thông tin", form save data + nhảy tab sang phase 3 (đã fix #14). Nhưng phase 2 vẫn chưa được đánh dấu "đã chốt" → arrow breadcrumb + tab chưa V xanh; pipelineLabel chưa update (vẫn "Đang chăm sóc" thay vì "Chờ book"). Buộc user phải bấm nút "Kết thúc phase 2" riêng.

**Fix**: `saveAndGoToBooking()` — sau save thành công, nếu `prevPhase ∈ {2,3}` và user có `CF_PHASE_CLOSE_PERM[prevPhase]` → tự động `closePhase($prevPhase, $viewer)` + refresh. Nếu thiếu perm hoặc closePhase throw → chỉ flash cf_error, không block flow.

**Verify tinker**: lead 12 phase=1 → set phase=2 → closePhase(2) → phase=3, closures=[1,2] ✅.

## 16. Relayout form đặt booking phase 3 thành 3 cột
**File**: `resources/views/components/leads/⚡lead-form.blade.php` (block "+ Tạo booking" ~line 2778-2971)

**Trước**: 4 hàng ngang:
- Hàng 1: Loại | Trạng thái | Ngày
- Hàng 2: Cơ sở | Phòng | Bác sĩ
- Hàng 3: Dịch vụ | Giờ | (trống)
- Hàng 4: Số liệu trình | Số lượng lọ | Dung tích lọ | 3 checkbox

**Sau** (theo yêu cầu sếp):
- Hàng 1: Loại | Trạng thái | Ngày (giữ)
- Hàng 2 (3 cột grid): **Địa điểm** (Cơ sở/Phòng/BS stacked) | **Nội dung** (Dịch vụ/Số lượng stacked) | **Thời gian** (Khung giờ)
- Hàng 3: 3 checkbox 1 hàng ngang (Có tư vấn | Có thăm khám lâm sàng | Kết hợp medical)

**Thay đổi field**:
- Gộp "Số liệu trình" + "Số lượng lọ" + "Dung tích lọ" → **1 field "Số lượng"** (dùng `newBookingSoLuongLo`, label = "Số lượng"). Ẩn UI 2 field kia; property PHP `newBookingSoLieuTrinh` + `newBookingDungTichLo` giữ default null để không vỡ payload sbooking (`SbookingClient::pushBooking` vẫn gửi `so_lieu_trinh=null`, `dung_tich_lo=null`).
- Note: nếu sếp muốn hẳn xoá 2 field khỏi DB/sbooking → cần đợt fix riêng (đụng schema + sbooking API contract).

**Thứ tự tương tác** vẫn tôn trọng rule check phòng của sbooking: Cơ sở → Phòng → (Dịch vụ, Bác sĩ) → Khung giờ. `availableSlots` chỉ load khi có `phong_id + dich_vu_id + ngày` — user vẫn phải chọn theo trình tự đó, chỉ khác về **vị trí trên UI** không đổi flow.

## 17. Chia 3 bucket booking: Khám lâm sàng / Tư vấn / Dịch vụ
**Cross-repo change**: lara-sbooking + lara-scrm.

**Sbooking side** (`F:/Laragon/www/lara-sbooking`):
- Migration `add_duoc_dat_tu_van_to_phong` — cột boolean default TRUE (mọi phòng đều tick sẵn theo yêu cầu PKD).
- `Phong` model fillable + cast.
- `LongevitySeeder`:
  - Reclassify các dv thăm khám sang `thuoc_nhom='kham_ls'` (Siêu âm, XQuang, Thăm khám tim mạch, Lấy máu — trước đây là 'khac').
  - Thêm 3 chuyên khoa mới `kham_ls`: **Khám Nội (sắp triển khai)**, **Khám Sản (sắp triển khai)**, **Khám Da liễu (sắp triển khai)**.
  - `seedPhong()` default `duoc_dat_tu_van=true` cho mọi phòng.
- `SettingsController` phong section — thêm toggle "Được đặt tư vấn".
- `SyncApiController` — push `duoc_dat_tu_van` khi scrm gọi `/sync/phong`.

**Sbooking form đặt lịch đã có sẵn radio 3 lựa chọn** ("Tư vấn / Thăm khám LS / Khác") — giữ nguyên, không sửa.

**Scrm side**:
- Migration `add_duoc_dat_tu_van_to_sb_rooms` — cột boolean default TRUE.
- `SbRoom` model fillable + cast.
- `SyncRoomsFromSbooking` — đọc + save `duoc_dat_tu_van`.
- `⚡lead-form.blade.php` block đặt booking phase 3:
  - `newBookingType` 2 → 3 giá trị: `kham_ls | tu_van | dich_vu`. Validation update.
  - UI dropdown 3 option: 🩺 Khám lâm sàng / 💬 Tư vấn / 💆 Dịch vụ.
  - Filter service list theo bucket: kham_ls→`la_dich_vu=false AND thuoc_nhom=kham_ls`, tu_van→`la_dich_vu=false AND thuoc_nhom=tu_van`, dich_vu→`la_dich_vu=true`.
  - Filter phòng theo bucket: `tu_van→duoc_dat_tu_van=true`, `kham_ls→kieu_phong=phong_kham`, `dich_vu→kieu_phong=phong_dich_vu`.
  - Đổi bucket auto reset phòng/dv/BS + reload rooms.
  - Save vào `booking_logs.type` map: `kham_ls|tu_van → 'tham_kham'`, `dich_vu → 'dich_vu'` (giữ enum 2 giá trị để không đổi schema + không đổi sbooking API contract).
- `SbookingClient::pushBooking` giữ mapping cũ (`type=='dich_vu' ? 'dich_vu' : 'phong_kham'`) — không đụng.

**Lệnh chạy sau khi pull code**:
```
cd F:/Laragon/www/lara-sbooking
php artisan migrate
php artisan db:seed --class=LongevitySeeder

cd F:/Laragon/www/lara-scrm
php artisan migrate
php artisan sb:sync-rooms
php artisan sb:sync-services   # nếu có command này để re-sync dv
```

## Test coverage
Dusk suite `tests/Browser/Hn/` (8 test, 11 assertions):
- H1-H7: verify next-step text 7 nguồn + panel Trường bổ sung ở phase 1
- H8: regression pool visibility (fix #5)

Chạy: `php artisan dusk tests/Browser/Hn/`

## Chưa cover (đã thống nhất với user, để pass sau)
- Đợt 1 full E2E booking → sbooking cho 7 nguồn (cần khảo sát DOM `lead-detail` phase 3).
- Đợt 2 filter list pages.
- Đợt 3 dashboard + báo cáo Observe.
- Đợt 4 xuất báo cáo.
