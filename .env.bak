APP_NAME=Lara-SCRM
APP_ENV=local
APP_KEY=base64:I/3OHJBwnJQ9C2t9MVzGxY/QVusc6SVwXlvIEb89xBU=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=lara_scrm
DB_USERNAME=root
DB_PASSWORD=

# Raw zone (PostgreSQL — DBngin)
DB_RAW_HOST=127.0.0.1
DB_RAW_PORT=5432
DB_RAW_DATABASE=lara_scrm_raw
DB_RAW_USERNAME=postgres
DB_RAW_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=reverb
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

REVERB_APP_ID=386252
REVERB_APP_KEY=sw03w4kwjyv8zburz38e
REVERB_APP_SECRET=ncuprptch742zt8dcms4
REVERB_HOST="localhost"
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"


# 2026-08-05: Shared secret cho callback từ lara-sbooking (BookingEventController).
#   Phải KHỚP với SCRM_API_TOKEN bên lara-sbooking/.env — nếu không, callback status
#   (da_xong, da_toi, tu_choi...) từ sbooking bị 401 → scrm không sync trạng thái booking.
BOOKING_API_TOKEN=demodemodemo123


# 2026-08-12 — AI-Coop (2 Anthropic API keys riêng cho route /ai-coop)
AI_COOP_KEY_A=
AI_COOP_KEY_B=
